#include <stdio.h>  // 导入头文件，包含基础函数，例如printf()等等

int main()  // 函数主入口，c程序只有一个主函数，每个程序都是从这里开始执行的
{
    printf("Hello World!\n");  // 输出，注意，函数定义不需要封号，里面的执行语句需要有封号
    return 0;  // 返回值，每个函数需要有返回值
}  // 每个代码块需要再函数名下方用大括号包围

/*
在vscode里面编译调试，文件名不可为中文！！！
*/
