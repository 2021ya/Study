age = int(input(":"))

if age >= 18:  # 判断age是否大于等于18
    print("ok")
    """
    == 是判断两个数值是否相等
    != 判断两个数值不相等
    >  判断数值大于
    <  判断数值小于
    >= 判断数值大于等于
    <= 判断数值小于等于
    """
else:  # 否则，上述不成立时执行下方代码块
    print("no")
